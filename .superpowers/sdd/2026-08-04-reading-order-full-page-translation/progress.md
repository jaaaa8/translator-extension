# SDD ledger — plan: docs/superpowers/plans/2026-08-04-reading-order-full-page-translation.md

Execution mode: subagent-driven; every subagent uses gpt-5.6-terra at medium reasoning.
Checkpoint policy: stop for user review after each Task, overriding continuous execution in the generic skill.

Model policy amended by user: Task 3–7 implementers use gpt-5.6-sol at max reasoning; task reviewers remain gpt-5.6-terra at medium reasoning.

Task 1: complete (commits 9237454..9b1d153, review clean)

Task 2: BLOCKED — pre-existing progressive-integration cancellation-metric race; Python 193 passed, Node baseline can finish request id-1 before releaseRequest records cancel_latency_ms; no Task 2 artifact or commit created.

Task 2a: complete (commit 0d47b5c, user-approved test-only timing stabilization).

Task 2: resumed and complete (commits 9b1d153..c35569a, review clean); Python 193 passed with test_ocr.py excluded, Node 9/9 passed, evaluator 98 passed, semantic policy batch count 25, control baseline frozen at merge commit 9b1d153.

Task 3: review changes requested — identical bbox geometry currently preserves shuffled input order, conflicting with the deterministic total-order requirement while ID/text/arrival tie-breaks are forbidden; awaiting contract ruling before fix round 1.

Task 3: minor (deferred): background-progressive fake-server translation-result queue is timing-sensitive under concurrent jobs; reproduced on BASE and current, not caused by Task 3 helper wiring.

Task 3: fix round 1/5 (1 addressed, 0 open — exact normalized-bbox dedupe, public helper duplicate guard, and dedupe cache-version bump; commits 04e695e..c806f14).

Task 3: complete (commits c35569a..c806f14, review clean; one deferred harness-flake Minor). Fresh verification: Python 25 passed, three Node gates passed, git diff --check passed; test_ocr.py not run.

Task 4: minor (deferred): popup test fixture creates readingDirection/currentLanguages mocks with ||=, so deleting the corresponding popup.html IDs would not fail this test.

Task 4: minor (deferred, reconfirmed): background-progressive fake translation FIFO assigns the queued 429 to whichever concurrent producer wins scheduling while the assertion pins it to rate-job.

Task 4: complete (commits c806f14..1ee4b07, review PASS with two deferred Minors). Fresh verification: five Node gates passed, Python 17 passed, git diff --check passed; test_ocr.py not run.

Task 6: review-deferred Finding 4 — normalize `start_scope.reading_direction` once before the job loop and decide the `scope_error` telemetry taxonomy; do not change Task 4 `acceptScope` behavior during this review-fix round.

Task 6: review-deferred Finding 6 — when wiring `orderPage()`, catch duplicate-geometry errors in the existing producer/job completion taxonomy with a machine-readable error code; do not allow an unhandled promise rejection.

Tasks 3–4 review fix: complete (commits `f84bc3b`, `a7dab1a`, `73c3c73`; independent Terra medium review PASS for spec compliance and task quality). Task 3 now documents/tests true image-intersection clamp, fully-outside-region rejection, connected-component chaining, RTL/LTR left-edge ordering, and intentional `stable_block_id(..., 0)` compatibility. Task 4 now isolates invalid rehydrated direction per job and sends direction explicitly in the popup action. Verification: Node 10/10, Python 28 passed, syntax/diff checks passed; `test_ocr.py` not run.

Tasks 3–4 external re-review: PASS. Full server gate excluding `server/tests/test_ocr.py`: 195 passed, 2 warnings. New Minor (deferred to Task 6): `background-progressive.test.js` uses one global fake translation-result FIFO, so two concurrent jobs can consume the queued 429 nondeterministically. Until Task 6 rewrites/removes the microbatch scenario, every Node gate for Tasks 5–6 must run the complete 10-file suite twice and report `2×10/10`; a single 10/10 run is insufficient evidence.

Workspace note: `MangaTranslatorBrowser/Welcome.md` deletion is user-authorized and unrelated to Spec B. Do not restore it and do not stage it in Spec B commits.

Task 5: complete (commit `fec60ac64069380a7a163b20f4df976d167e2cbb`; independent Terra medium review PASS for spec compliance and task quality, no findings). ES/PT remain separate public languages and OCR cache identities, but share one class-keyed `PaddleLatinEngine` instance pinned to `lang="es"`, `PP-OCRv6`; production language/version coverage and acceptance version shape include PT. Fresh controller verification: server `199 passed, 2 warnings` with `server/tests/test_ocr.py` explicitly excluded; full Node suite `2×10/10`; fake probe confirms shared instance, one registry init, and exact Paddle kwargs; `git diff --check` clean. `server/tests/test_ocr.py` received only the static language expectation update and was not run. Plan inventory omission `server/acceptance_app.py` was limited to one required PT recognizer entry.

Task 5 external re-review: PASS with one non-blocking Minor deferred to Task 6. `server/acceptance_app.py` advertises PT in `/health.versions.recognizers`, but the `/ocr` and `/translate-items` acceptance routes still reject PT through legacy `{"ja", "es"}` allowlists (current lines 350 and 406). Task 6 already modifies this harness; change exactly both literals to include `"pt"` and retain the existing `"fr"` unsupported-language gate. No Task 5 production fix or new commit was made at this checkpoint.

Task 6: in progress from BASE `fec60ac64069380a7a163b20f4df976d167e2cbb`. Carry-ins: fix the two acceptance PT allowlists; normalize scope-level direction before the job loop with the approved error taxonomy; map duplicate-geometry/order failures into a machine-readable producer/job failure instead of an unhandled rejection; remove the known global microbatch FIFO race through the approved five-scenario full-page rewrite. Task 6 verification must include the complete 10-file Node suite twice (`2×10/10`) and must never run `server/tests/test_ocr.py`.

Task 6: complete (commits `fec60ac..a37fbde`, independent Terra medium review clean for spec compliance and task quality). Shared strict `/translate-items` models/handler, page-context prompt, `comic-page-items-v2`/`full-page-v1`, OCR-complete ordered full-page translation, atomic response/cache/render, retirement semantics, scope-level direction taxonomy, reading-order/dimension error codes and PT acceptance allowlists are implemented. The microbatch queue/timers/state/phase were removed atomically and exactly the five named scenarios were rewritten. Fresh controller verification: server `211 passed, 2 warnings` with `server/tests/test_ocr.py` explicitly excluded; full Node suite `2×10/10`; deletion/static tripwire PASS; control baseline unchanged; `git diff --check` clean; staging empty. Stop at the Task 6 checkpoint; Task 7 has not started.

Task 6 external review: PASS with two non-blocking Minors, both deferred and not silently treated as fixed. (1) `replayPage()` currently replays translations only for `cacheHit`; a consumer attached to an in-flight producer after `page.state` becomes `complete` but before persistence can receive successful `image_done` without translation events. Proposed correction: replay when `cacheHit || page.state === "complete"`, with a regression test that holds completion persistence; partial-hot remains blocked. (2) the scenario name `partial page replays complete blocks and requests only missing IDs` contradicts its new full-page assertions; rename it to describe requesting the complete ordered page without replaying cached blocks. No production/test edit or new commit was made in response to these Minor findings; final whole-branch triage must not drop them.

Task 7: in progress from BASE `a37fbdec0dd2dd2d717a9fd754a07b98a475540b`. Scope is verification and the Spec B worklog only: full server gate with `server/tests/test_ocr.py` explicitly excluded, one complete sequential Node suite, offline real-page evaluator, semantic batch-count 25, frozen-control/version/removal/scenario audits, then an observed-results checkpoint commit. The two Task 6 Minors remain deferred; Task 7 must not modify production or test behavior.

Task 7: complete (commit `18aa2f8ef435d91b92494495119583e9cfda05a2`; independent Terra medium review PASS with no findings). Commit scope is exactly the Spec B worklog and records implementation commit `a37fbdec0dd2dd2d717a9fd754a07b98a475540b`. Fresh controller verification: server `211 passed, 2 warnings` with explicit `--ignore=server/tests/test_ocr.py`; Node `10/10`; offline evaluator `98 passed`; semantic policy batch count `25`; frozen control clean; versions/removal/static assertions pass; staging empty. This is only the offline vertical-slice checkpoint: runtime telemetry/live quality remain unclaimed. Both Task 6 Minors remain deferred. Stop before Task 8.

Post-Task 7 inline fix: complete (commit `8a4b08d`, direct implementation per user request). Closed both deferred Task 6 Minors: a late consumer now replays translations from an in-memory page already marked `complete` while persistence is still pending, and the partial-hot scenario name now matches its full-page assertions. TDD observed the regression RED as `actual []` versus expected `["b1"]`, then GREEN; full Node suite passed `2x10/10` before commit and `10/10` after commit. Only `extension/background.js` and `extension/test/background-progressive.test.js` were committed; no Python/server test was needed or run, and `server/tests/test_ocr.py` remained untouched/not run. The Task 7 worklog remains the historical offline checkpoint for implementation commit `a37fbde`; no live-quality claim was added.

External review of `18aa2f8` and `8a4b08d`: PASS. Documentation finding closed in commit `bec403b`: the worklog preserves the Task 7 implementation pin at `a37fbde` and records `8a4b08d` as a verified post-checkpoint fix; the canonical Obsidian note received an append-only dated correction. Fresh HEAD verification: server `211 passed, 2 warnings` with explicit `--ignore=server/tests/test_ocr.py`, Node `10/10`. One pre-existing race remains open for final whole-branch triage: a consumer attaching to a terminal `partial` producer after the existing consumer completion loop but before awaited job cleanup/map deletion can miss `image_done`/`scope_done` and leak its request. Proposed fix is to remove the producer from `producers` before awaiting `removeProducerJobs()` in both `finishProducer()` and `failProducer()`, with a dedicated regression test; no production change was made for this follow-up.

Task 8: in progress from BASE `bec403b37253e4c5be18061b6baf971993a80b20`. Scope is runtime evidence and the Spec B worklog only: capture real extension flows for JA RTL cold/warm, manual/synthetic LTR, and PT shared-engine behavior; record exact observed metrics or a machine-readable blocked reason; preserve the frozen control and privacy constraints. Implementer and reviewer use `gpt-5.6-terra` at medium reasoning. The open terminal-partial producer cleanup race remains a final whole-branch triage item and must not be silently folded into telemetry capture.

Task 8: BLOCKED before runtime capture. Browser control rejected `chrome://extensions/`, so the extension could not be reloaded or verified as loaded from `D:\MangaTranslator\extension`; JA RTL cold/warm, manual LTR, and PT shared-engine runs were not attempted because they would not prove the checkout under review. The Spec B worklog records machine-readable reason `chrome_extensions_reload_blocked_by_browser_url_policy`; no commit or review was created. Frozen control and privacy checks stayed clean, and both task-owned servers were stopped. Resume only after the user manually reloads/verifies the unpacked extension from the required path.

Task 8: review changes requested after runtime commit `b46b716`. Important findings: `manual_ltr` and `pt` must record the observed cache taxonomy/page cache-hit state; PT request evidence must retain the exact public field name `src_lang`; the shared-Latin conclusion must be bounded to the same-PID Paddle marker observation rather than assert a unique engine instance from logs alone. Fix round 1 is in progress; production/control remain untouched.

Task 8: fix round 1/5 (3 addressed, 0 open; commit `78bcf2f`). Added observed LTR/PT cache taxonomy and page cache-hit state, retained exact PT public request fields, and bounded the shared-Latin conclusion to same-PID marker behavior. Scoped Terra medium re-review found no new breakage or out-of-scope findings.

Task 8: complete (commits `bec403b..78bcf2f`, review clean). Runtime worklog records four network full-page traces, a translation-memory hot empty trace, explicit LTR popup-to-request evidence, PT public request evidence, and bounded same-process PT/ES Paddle marker observation. Frozen control/privacy/timing/commit-scope checks are clean; task-owned servers are stopped. Final whole-branch review remains before the user checkpoint.

Final whole-branch review: fixes required. Important merge-blocker confirmed: late consumer can attach to a terminal `partial` producer after the completion loop but before awaited job cleanup/map deletion, then miss `image_done`/`scope_done` because `producer.enqueued` prevents rescheduling. Minor: popup harness fallback can mask deletion of the real direction/current-language elements. One final fix wave is in progress; it must use a focused RED/GREEN regression, close both `finishProducer` and `failProducer` windows, tighten the popup test, and update the persistent follow-up status without touching frozen control/live quality.

Final whole-branch review follow-up: resolved-after-final-review (implementation commit `3fc910d86c4a32b959b92dbd4a570a4cd969ceda`). `finishProducer()` and `failProducer()` now remove the terminal producer from `producers` immediately after completing their current consumers and before awaited job cleanup. Focused regression holds the real `removeJob` cleanup await-window, attaches a late consumer to terminal partial and failed producers, and observes new `image_done` plus `scope_done`; moving either deletion back after cleanup reproduces the timeout. Popup harness now asserts the parser-created `readingDirection` and `currentLanguages` elements instead of supplying fallbacks. This is a post-runtime control fix only: frozen control/runtime numbers and live-quality claims remain unchanged.
