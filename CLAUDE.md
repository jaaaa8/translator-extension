# MangaTranslator — hướng dẫn dự án

## Obsidian vault

Thư mục `MangaTranslatorBrowser/` là một **Obsidian vault**. File tài liệu/tiến độ chính là `MangaTranslatorBrowser/Tiến độ MangaTranslator.md`.

**Rule:** Trước khi đọc hoặc chỉnh BẤT KỲ file `.md` nào trong `MangaTranslatorBrowser/`, gọi skill Obsidian phù hợp TRƯỚC, rồi mới thao tác:
- Sửa nội dung ghi chú (callout `> [!note]`, link `[[...]]`, frontmatter, markdown): `obsidian:obsidian-markdown`.
- Thao tác vault qua CLI (tạo/di chuyển note, mở, search theo API Obsidian): `obsidian:obsidian-cli`.
- Canvas `.canvas`: `obsidian:json-canvas`. Bảng Bases: `obsidian:obsidian-bases`.

Chỉ bỏ qua khi user nói rõ "sửa trực tiếp, khỏi dùng skill".

## Ngôn ngữ

Trả lời + tài liệu dự án bằng **tiếng Việt**.
